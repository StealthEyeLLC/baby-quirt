{
  "name": "@stealtheye/baby-quirt",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "start": "node src/main.mjs",
    "client": "node src/client.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "release:build": "bash scripts/build-release.sh",
    "release:verify": "bash scripts/verify-release.sh",
    "ci": "npm run check && npm test && npm run release:build && npm run release:verify"
  }
}
