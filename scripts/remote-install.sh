#!/usr/bin/env bash
# Retired v1 SSH installer. Standalone v2 uses the Baby-owned fixed controller.
set -euo pipefail
echo "ERROR: remote installation is superseded by the standalone Baby deployment controller" >&2
exit 64
