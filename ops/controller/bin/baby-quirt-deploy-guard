#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
RELEASE_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)
NODE_BIN=${BABY_QUIRT_NODE_BIN:-/opt/node-v24.18.0-linux-x64/bin/node}
exec "$NODE_BIN" "$RELEASE_ROOT/lib/dist/controller/cli.js" "$@"
